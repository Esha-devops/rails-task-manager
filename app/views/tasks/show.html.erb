<h1>Task Details</h1>

 <%= @task.title %></p>
 <%= @task.details %></p>
 <%= @task.completed %></p>
<%= link_to 'Back to Tasks', tasks_path %>
<%= link_to "Delete",
 task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
